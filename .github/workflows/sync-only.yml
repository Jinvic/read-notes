# .github/workflows/sync-only.yml
name: Sync Only (from Hugo)

on:
  workflow_dispatch:
    inputs:
      hugo_repo:
        description: 'Hugo仓库名称'
        required: true
      hugo_commit:
        description: 'Hugo提交哈希'
        required: true
      sync_reason:
        description: '同步原因'
        required: true
      notes_json:
        description: '需要同步的笔记配置(JSON)'
        required: true

# 并发控制
concurrency:
  group: sync-only-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sync-notes:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 记录触发信息
        run: |
          echo "🔄 同步任务开始"
          echo "来源: ${{ github.event.inputs.hugo_repo }}"
          echo "提交: ${{ github.event.inputs.hugo_commit }}"
          echo "原因: ${{ github.event.inputs.sync_reason }}"
          
      - name: 检出MkDocs仓库
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.HUGO_TOKEN }}
          fetch-depth: 0
          
      - name: 设置Python环境
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'
          cache: 'pip'
          
      - name: 安装Python依赖
        run: |
          pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pyyaml frontmatter
          
      - name: 解析同步配置
        id: parse-config
        run: |
          # 将输入的JSON保存到文件
          echo '${{ github.event.inputs.notes_json }}' > sync-notes.json
          
          # 统计笔记数量
          NOTE_COUNT=$(python -c "import json; data=json.load(open('sync-notes.json')); print(len(data))")
          echo "📚 需要同步 $NOTE_COUNT 篇笔记"
          
          # 设置输出变量
          echo "note_count=$NOTE_COUNT" >> $GITHUB_OUTPUT
          
      - name: 从Hugo仓库拉取内容
        env:
          HUGO_TOKEN: ${{ secrets.HUGO_TOKEN }}
        run: |
          echo "⬇️  正在从Hugo仓库拉取内容..."
          
          # 创建临时目录
          mkdir -p hugo-temp
          
          # 克隆Hugo仓库（最小化克隆）
          git clone \
            --depth 1 \
            "https://x-access-token:$HUGO_TOKEN@github.com/${{ github.event.inputs.hugo_repo }}" \
            hugo-temp
          
          cd hugo-temp
          
          # 检出指定提交
          git checkout ${{ github.event.inputs.hugo_commit }}
          
          echo "✅ Hugo仓库内容已拉取"
          echo "提交哈希: $(git rev-parse HEAD)"
          
      - name: 同步笔记和图片到src目录
        id: sync-step
        run: |
          echo "🔄 开始同步笔记和图片..."
          
          # 创建必要的目录
          mkdir -p src
          
          # 运行同步脚本（只同步到src目录，不转换）
          python scripts/sync_from_hugo.py \
            --hugo-dir ./hugo-temp \
            --config sync-notes.json \
            --src-dir ./src \
            --log-level INFO
          
          # 检查同步结果
          if [ -d "src" ]; then
            echo "✅ src目录已创建"
            
            # 统计文件数量
            MD_FILES=$(find src -name "*.md" -type f | wc -l)
            IMAGE_FILES=$(find src -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" -o -name "*.gif" -o -name "*.svg" -o -name "*.webp" -o -name "*.bmp" | wc -l)
            JSON_FILES=$(find src -name "*.json" -type f | wc -l)
            
            echo "📄 Markdown文件: $MD_FILES 个"
            echo "🖼️  图片文件: $IMAGE_FILES 个"
            echo "📋 JSON文件: $JSON_FILES 个"
            
            if [ $MD_FILES -gt 0 ]; then
              echo "has_changes=true" >> $GITHUB_OUTPUT
              echo "md_count=$MD_FILES" >> $GITHUB_OUTPUT
              echo "image_count=$IMAGE_FILES" >> $GITHUB_OUTPUT
              
              # 显示文件结构
              echo ""
              echo "📂 文件结构预览:"
              if command -v tree &> /dev/null; then
                tree src --dirsfirst -L 3 || echo "tree命令不可用"
              else
                find src -type f | head -20
              fi
              
              # 显示具体文件
              echo ""
              echo "📄 Markdown文件列表:"
              find src -name "*.md" -type f | while read f; do
                size=$(stat -f%z "$f" 2>/dev/null || stat -c%s "$f" 2>/dev/null || echo "?")
                echo "  - $f ($size bytes)"
              done
              
              if [ $IMAGE_FILES -gt 0 ]; then
                echo ""
                echo "🖼️  图片文件列表:"
                find src -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" -o -name "*.gif" -o -name "*.svg" -o -name "*.webp" -o -name "*.bmp" | head -10 | while read f; do
                  size=$(stat -f%z "$f" 2>/dev/null || stat -c%s "$f" 2>/dev/null || echo "?")
                  echo "  - $f ($size bytes)"
                done
                if [ $IMAGE_FILES -gt 10 ]; then
                  echo "  ... 还有 $((IMAGE_FILES - 10)) 个图片"
                fi
              fi
            else
              echo "❌ src目录中没有Markdown文件"
              echo "has_changes=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "❌ src目录未创建"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi
          
      - name: 提交同步结果
        if: steps.sync-step.outputs.has_changes == 'true'
        run: |
          echo "📝 提交同步结果到Git..."
          
          # 配置Git用户
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          # 添加所有变更
          git add src/
          
          # 检查是否有实际变更
          if git diff --cached --quiet; then
            echo "ℹ️  没有实际变更，跳过提交"
            echo "has_actual_changes=false" >> $GITHUB_OUTPUT
          else
            # 提交变更
            git commit -m "sync: 从Hugo同步笔记 [${{ github.event.inputs.hugo_commit }}]
            
            来源: ${{ github.event.inputs.hugo_repo }}
            原因: ${{ github.event.inputs.sync_reason }}
            笔记数: ${{ steps.parse-config.outputs.note_count }}
            图片数: ${{ steps.sync-step.outputs.image_count || 0 }}"
            
            # 推送变更
            git push origin ${{ github.ref_name }}
            
            echo "✅ 已提交并推送同步结果"
            echo "has_actual_changes=true" >> $GITHUB_OUTPUT
            echo "commit_hash=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
          fi
          
      - name: 清理临时文件
        if: always()
        run: |
          echo "🧹 清理临时文件..."
          rm -rf hugo-temp sync-notes.json
          # 如果有同步报告也清理
          if [ -f "sync-report.json" ]; then
            rm -f sync-report.json
          fi
          
      - name: 生成最终报告
        if: always()
        run: |
          echo ""
          echo "📊 同步任务最终报告"
          echo "===================="
          echo "任务状态: ${{ job.status }}"
          echo "触发来源: ${{ github.event.inputs.hugo_repo }}"
          echo "同步原因: ${{ github.event.inputs.sync_reason }}"
          echo "配置笔记数: ${{ steps.parse-config.outputs.note_count }}"
          echo "检测到变更: ${{ steps.sync-step.outputs.has_changes || 'false' }}"
          echo "Markdown文件: ${{ steps.sync-step.outputs.md_count || 0 }}"
          echo "图片文件: ${{ steps.sync-step.outputs.image_count || 0 }}"
          echo "实际提交: ${{ steps.sync-step.outputs.has_actual_changes || 'false' }}"
          
          if [[ "${{ job.status }}" == "success" ]]; then
            if [[ "${{ steps.sync-step.outputs.has_changes }}" == "true" ]]; then
              echo ""
              echo "🎉 同步成功完成!"
              if [[ "${{ steps.sync-step.outputs.has_actual_changes }}" == "true" ]]; then
                echo "✅ 文件已提交到Git仓库"
              else
                echo "ℹ️  文件已同步但未提交（可能无实际变更）"
              fi
            else
              echo ""
              echo "ℹ️  同步完成，但未检测到变更"
            fi
          else
            echo ""
            echo "❌ 同步任务失败"
            echo "请检查上面的错误日志"
          fi
          
      - name: 触发转换工作流（可选）
        if: steps.sync-step.outputs.has_actual_changes == 'true'
        run: |
          echo "🚀 同步完成，准备触发转换工作流..."
          # 这里可以添加触发convert.yml工作流的代码
          # 或者等手动触发