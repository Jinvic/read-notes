# 在 MkDocs 仓库创建 .github/workflows/sync-from-hugo.yml
name: Sync from Hugo

on:
  workflow_dispatch:
    inputs:
      hugo_repo:
        description: 'Hugo仓库名称'
        required: true
      hugo_commit:
        description: 'Hugo提交哈希'
        required: true
      sync_reason:
        description: '同步原因'
        required: true
      notes_json:
        description: '需要同步的笔记配置(JSON)'
        required: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 测试接收
        run: |
          echo "🎉 成功接收到 Hugo 仓库的触发!"
          echo "Hugo 仓库: ${{ github.event.inputs.hugo_repo }}"
          echo "同步原因: ${{ github.event.inputs.sync_reason }}"
          
          # 解析 notes_json
          echo '${{ github.event.inputs.notes_json }}' > notes.json
          echo "需要同步的笔记:"
          cat notes.json | jq -r '.[].source'